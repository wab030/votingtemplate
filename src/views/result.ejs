<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <style>
        body { font-family: sans-serif; margin: 50px; text-align: center; background-color: #f4f4f9; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 30px; border-radius: 12px; background-color: #fff; box-shadow: 0 4px 8px rgba(0,0,0,0.1); }
        h1 { color: #28a745; }
        p { font-size: 1.2em; color: #6c757d; }
        .details { margin-top: 20px; padding: 15px; background-color: #e9f7ef; border-radius: 8px; border-left: 5px solid #28a745; }
        .details strong { color: #333; }
        a { color: #007bff; text-decoration: none; margin-top: 20px; display: inline-block; }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= title %></h1>
        <p><%= message %></p>
        
        <% if (data) { %>
            <div class="details">
                <p><strong>Candidato:</strong> <%= data.nome_candidato %></p>
                <p><strong>Votos:</strong> <%= data.votos %></p>
            </div>
        <% } %>

        <% if (confirmation) { %>
             <div class="details">
                <p><strong>Você votou em:</strong> <%= confirmation.candidato %></p>
            </div>
        <% } %>

        <a href="/">Voltar para o Início</a>
    </div>
</body>
</html>